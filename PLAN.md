# 实证方案与路径（基于现有数据）

**Summary**  
在现有数据基础上构建2000–2024年地级市面板，围绕“财政缺口/债务压力 → 政府引导基金（规模、阶段偏好）→ 创新产出”的逻辑，形成完整可执行的实证路径：基准回归、机制检验（H2/H4/H5）、异质性、内生性与稳健性。主因变量采用 `发明授权量(ln1p)`，债务压力采用 `债务/财政收入` 与 `债务/GDP` 两种口径并行。H3（挤出市场VC）因缺乏VC数据，明确为局限并给出未来数据补全路径。

**Key Changes / Implementation Steps**  
1. **变量构造与数据整理（城市-年份面板）**
   - **创新产出（因变量）**  
     - 来自 `各省市专利获得情况.csv` 的城市-年 `Invg`（发明授权量），构造 `ln1p(Invg)` 为主因变量。  
     - 备选：`各省市专利申请情况.csv` 中 `Inva`（发明申请量）等。
   - **核心解释变量**  
     - **财政缺口**：`(一般公共预算支出 - 一般公共预算收入) / 一般公共预算收入`，分别从 `地级市财政支出.csv` 与 `地级市财政收入.csv` 转长面板。  
     - **债务压力**：`地方政府债务余额 / 一般公共预算收入` 与 `地方政府债务余额 / GDP`（两套口径），债务来自 `地方政府债务：地级市：余额.csv`。  
   - **政府引导基金（中介/机制）**  
     - 使用 `政府引导基金整合数据/城市_投资统计_分年份_2000-2024.csv` 的城市-年指标：  
       - 规模：`总投资金额`、`总投资数量`  
       - 阶段偏好：`早期投资占比_数量/金额`、`广义早期占比_数量/金额`  
   - **控制变量（城市-年）**  
     - `GDP`、`人均GDP`、`常住人口`、`第二产业`、`实际利用外资`、`贷款余额`、`科学支出`、`财政透明度`等（各宽表转长）。  
     - **市场化指数**为省级年指标（`1997-2024年市场化指数和各分项指数 的副本.csv`），按省份-年合并到城市-年。
   - **统一清洗规则**  
     - 处理宽表头/脚元信息行（保留年份列），年份统一为整数；  
     - 统一省份/城市命名（去掉“市”“地区”等后缀，必要时人工匹配表）；  
     - 金额单位统一（万元/亿元换算）并记录；  
     - 对极端值做1%或5% winsorize（用于稳健性）。

2. **基准回归（H1）**  
   - 基准模型（城市+年份双固定效应，城市聚类稳健标准误）：  
     - `ln1p(Innovation_{i,t}) = β * FiscalGap_{i,t-1} + Controls_{i,t-1} + μ_i + λ_t + ε_{i,t}`  
     - `ln1p(Innovation_{i,t}) = β * DebtPressure_{i,t-1} + Controls + FE`  
   - 关键解释变量采用滞后一期，减少反向因果。

3. **机制检验（H2/H4/H5）**  
   - **H2 风险偏好/早期阶段**：  
     - 以 `早期投资占比` 为因变量回归财政缺口/债务压力。  
     - 预期：压力上升 → 早期占比下降。  
   - **H4 耐心资本/长期性**：  
     - 以 `广义早期占比` 或 `中后期/成熟期占比` 作为替代，检验投资阶段向短周期倾斜。  
   - **H5 政府引导基金规模**：  
     - 以 `总投资金额` / `总投资数量` 为因变量，检验压力→规模下降。  
   - **中介路径**：  
     - 分步回归或Sobol/中介效应检验：`压力 → GGF指标 → 创新产出`。

4. **异质性分析**  
   - 市场化水平高/低（按省级市场化指数分组）；  
   - 政府透明度高/低（城市-年财政透明度）；  
   - 区域（东/中/西/东北）或城市规模（按人口或GDP分位）分组。

5. **内生性与稳健性**  
   - **内生性**：  
     - 解释变量滞后；  
     - 事件冲击：以2014《预算法》与2015债务置换为时间断点，做高负债城市 vs 低负债城市的DID（处理组按2013年前债务压力分组）。  
   - **稳健性**：  
     - 替换因变量（发明申请量、授权总量）；  
     - 替换解释变量（债务/收入 vs 债务/GDP；财政缺口口径）；  
     - 控制变量集合变化；  
     - 不同winsorize阈值；  
     - 剔除一线/计划单列城市。

6. **H3 说明（挤出市场VC）**  
   - 当前数据缺乏市场VC投资，因此该机制无法直接检验；  
   - 建议未来补充：清科/投中/企名片等城市-年VC投资数据，或使用企查查/天眼查融资事件构建市场VC强度变量。

**Test Plan / Validation**  
1. 数据一致性检查：  
   - 城市-年面板是否完整覆盖 2000–2024；  
   - 关键变量缺失比例；  
   - 财政数据、债务数据、创新数据是否都能在同一城市-年上对齐。  
2. 统计描述：  
   - 主要变量均值/分位数/相关性；  
   - GGF指标与创新指标简单相关性方向是否符合预期。  
3. 回归结果合理性：  
   - 基准回归符号、显著性；  
   - 机制回归结果是否与假设一致；  
   - 异质性结果是否有经济解释。  

**Assumptions / Defaults**  
- 采用 `发明授权量` 的 `ln1p` 作为主因变量。  
- 债务压力使用两种口径并行：`债务/财政收入`、`债务/GDP`。  
- 关键解释变量滞后一期作为默认设定。  
- H3 作为局限说明，不做实证检验，提出可执行的未来数据补充方案。  
